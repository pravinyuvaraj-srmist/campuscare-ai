# Backend

Planned stack: Python + FastAPI.

Initial API:

- `GET /health`
- `POST /api/chat`

The backend will orchestrate retrieval and AI generation while keeping provider credentials in environment variables.
